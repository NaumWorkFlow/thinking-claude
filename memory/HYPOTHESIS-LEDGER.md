# Реестр гипотез

> Каждое важное `[Предположение]` — строка здесь. Статусы: `непроверено` → `подтверждается` → `[Факт]` ИЛИ `опровергнуто → убрать`.
> Это механизм само-улучшения: гипотезы стареют в факты или отсеиваются на данных, а не на вере.

> **Эпоха (P0-1, FABLE5-REVIEW.md).** Вердикты keep/drop/tweak привязаны к поколению модели-исполнителя и модели-судьи — критерий выживания модуля («переживёт ли переход на более сильную модель?», `_build/graveyard/GRAVEYARD.md`) требует ре-теста при смене эпохи, а не переноса старого вердикта по умолчанию. **Все строки ниже, до этой правки (2026-07-02) — эпоха `opus-4.x / sonnet-4.6, до Fable 5`.** Задним числом ре-тест не проводился (нужен человек-судья через `/test-module`/`/harness-probe`) — вердикты остаются в силе для этой эпохи, но помечены как ждущие ре-базлайна на новой эпохе.
>
> **Примечание 27.07.2026:** сам документ `FABLE5-REVIEW.md` в репозитории не сохранён (проверено поиском по дереву и истории git) — ссылки на него в этом файле и в `PROJECT-MEMORY.md` историчны. Его содержание отражено в `CHANGELOG.md` (релиз 1.1.0) и в гипотезах H14–H15.

| ID | Утверждение | Основание | Предсказание (что увидим, если верно) | Статус | Свидетельства | Решение |
|----|-------------|-----------|----------------------------------------|--------|---------------|---------|
| H1 | Методология в инструкциях проекта Cowork заметно меняет поведение Клода | Тест «$1M» | Честный спор, эпистемические метки, уточняющие вопросы вместо «жвачки» | подтверждается | Ответ на «$1M» показал все 4 признака | оставить, копить наблюдения |
| H2 | В Cowork метки `[Факт]` появляются, но без источников, т.к. скилл `fact-mode` не загружен (только ядро) | Тот же тест: «90%+ трейдеров проигрывают `[Факт]`» без ссылки; прогноз помечен `[Факт]` вместо `[Вывод]` | После установки `fact-mode` как скилла факты получат источники и корректные метки | непроверено | — | проверить при роллауте fact-mode |
| H3 | Порционный роллаут с человеком-судьёй (A/B) эффективнее, чем big-bang всех модулей | Verifier-first + проблема атрибуции | Быстрее находим, какой модуль реально помогает; меньше скрытых конфликтов | непроверено | — | проверять по ходу Этапа 2 |
| H4 | Харнес `/test-module` должен уметь набросать недостающий модуль, а не только тестировать существующий | Первый прогон `/test-module roles`: модуль не существовал как файл, тест встал | После патча draft-fallback тест запускается даже для модуля «из блупринта» | подтверждается → [Факт] 27.07.2026 | Прогон 1: честный `[Не найдено]`, методология не выдумала тест; команда пропатчена | оставить патч, проверить на след. прогоне |
| H5 | Модуль `roles` улучшает результат на доменных/стратегических задачах, но вытесняет кросс-доменные аналогии | A/B-тест roles, кейсы 1–3 | Кейс 1 B>A (юр.риск + конфликт); кейс 3 value-gate сработал; кейс 2 регрессия — потеря аналогий Strava/Duolingo | подтверждается → keep | Голоса: B / ничья(A) / без разницы | **keep + tweak применён:** добавлено правило «не терять аналогии»; перепроверить на след. прогоне |
| H6 | Модуль `adjacent-fields` добавляет ценные именованные кросс-доменные переносы и проходит value-gate | A/B-тест, кейсы 1–3 | Кейс 1 B>A (переносы из медицины/финансов); кейс 2 B≈A, но оригинальнее; кейс 3 value-gate сработал | подтверждается → keep | Голоса: B / B(маржин.) / без разницы | keep, без правок |
| H6w | (watch) `adjacent-fields` может рефлекторно отбрасывать этич./ценностные переносы как «дубль» | В кейсах 1–2 отброшены религия/этика | На задаче про долгую репутацию/бренд проверить, не теряет ли он ценный перенос (пример: «японский маркетинг») | непроверено | — | проверить на профильной задаче; патчить только при реальном сбое |
| H7 | Модуль `pre-mortem` улучшает план (приоритизация рисков + ранние индикаторы) и не дублирует red-team | A/B-тест, кейсы 1–3 | Кейс 1 B>A (таблица вероятность×ущерб + индикаторы); кейс 2 B≈A; кейс 3 value-gate сработал | подтверждается → keep | Голоса: B / B(маржин.) / без разницы | keep, без правок |
| H8 | Sonnet 4.6 достаточно для харнеса/рутины Claude Code; Opus только для тяжёлого суждения (M8 в деле) | Харнес — структурная работа, не глубокое рассуждение; суждение делают человек + Opus в Cowork | Качество тестов не падает, расход токенов ~5× ниже | подтверждается (цена) → [Факт] 27.07.2026 | Токены быстро тратились на Opus | Claude Code → Sonnet для тестов/рутины; Opus по требованию |
| H9 | Модуль `discovery` ведёт реальный цикл генерация→оценка→мутация при наличии верификатора и честно уходит в режим гипотез без него | A/B-тест, кейсы 1–2 | Кейс 1: цикл + 2-opt +4%; кейс 2: Шаг 0 «нет верификатора» → `[Предположение]` + proxy | подтверждается → keep | Голоса: B / B | keep, без правок |
| H10 | Харнес для верифицируемых задач должен фиксировать ОБЩИЙ тест-набор для A и B | Прогон discovery: A и B получили разные наборы → штрафы несравнимы | После патча числа A/B сравнимы | подтверждается → применено → [Факт] 27.07.2026 | команда `test-module` пропатчена | оставить |

## Как добавлять запись
1. Сформулируй утверждение и предсказание (фальсифицируемое — что должно произойти, если оно верно).
2. Укажи основание и стартовый статус `непроверено`.
3. По мере работы дописывай свидетельства и двигай статус.
4. Подтвердилось на данных → перенеси суть в `PROJECT-MEMORY.md` как решение/факт. Опровергнуто → решение «убрать», коротко зафиксируй почему.

---

## Батч независимого оценщика (2026-06-28)
*Эпоха: opus-4.x (судья) / sonnet-4.6 (исполнитель), до Fable 5. Не ре-тестировано на новой эпохе.*

Прогон через беспристрастного судью (Opus, отдельная сессия). **Эти ре-тесты ОТМЕНЯЮТ мои ранние само-судейские оценки H5–H7, H9** (они были предвзяты — я и строил, и судил).

| Модуль | Вердикт | Ключевая правка |
|---|---|---|
| fact-mode | KEEP (сильнейший) | — (ловит фабрикацию URL/оверконфиденс) |
| verification | KEEP | убрать дрейф «верификатор чинит сам»; обработка отсутствия DoD |
| roles | KEEP | твик подтверждён (аналогии не теряются); ценность умеренная |
| divergent | KEEP | — |
| landscape | KEEP+tweak | молчаливый гейт; gap как [Предположение]; не резать покрытие |
| alternatives | KEEP+tweak | сузить триггер; не терять структуру/числа |
| adjacent-fields | KEEP+tweak | молчаливый гейт; дедуп переносов |
| discovery | KEEP+tweak | cost-gate (не гонять цикл, где хватает one-shot) |
| pre-mortem | KEEP+tweak | **приоритет:** гейт не срабатывать на коде |
| steelman | KEEP+tweak | гейт не срабатывать на коде |
| amplifier | KEEP провизорно | нужен дифф-тест на латентных дефектах, иначе мерж в red-team |
| benchmark | **DROP-кандидат** | + 1 подтверждающий тест; остаток мержить в fact-mode |
| prioritization | **DROP-кандидат** | + 1 подтверждающий тест |

**Сквозные находки:**
- **Value-gate — системный разнобой.** Молчат правильно (эталон): alternatives, discovery, fact-mode, roles, divergent, amplifier, prioritization. Проговаривали гейт: landscape, adjacent-fields. Misfire (исполнялись на коде/учебном): pre-mortem, steelman, benchmark. → Исправлено сквозным правилом в `CLAUDE.md` + пер-модульными гейтами.
- **Само-активация базы.** На многих задачах базовая модель сама воспроизводит поведение модуля. Следствие: маржинальная ценность таких модулей = *enforcement*, а не введение поведения; тестировать их надо на кейсах, где база **проваливается** (как fact-mode с фабрикацией URL).

**Остаётся 3 подтверждающих теста (на кейсах, где база НЕ self-активируется):** benchmark, prioritization (→ keep или delete), amplifier (→ keep или мерж в red-team).

### Подтверждающие ретесты (2026-06-28) — закрывают спорную тройку

| Модуль | Было | Стало | Свидетельство | Условие keep |
|---|---|---|---|---|
| benchmark | DROP-кандидат | **KEEP+TWEAK** | 2 чистых выигрыша при нетривиальном подборе эталона (Toptal vs Notion; LYT vs PARA); гейт починен | триггер сужен до неочевидного выбора эталона |
| amplifier | KEEP провизорно | **KEEP** | поймал зарытое high-severity допущение (биллинг в плане), которое база пропустила | активировать на зарытых допущениях; watch overlap с pre-mortem/red-team |
| prioritization | DROP-кандидат | **KEEP+TWEAK условный** (слабейший) | 1 transparency-win (expected-value на конфликте метрик) + 1 несрабатывание на 15 задачах | сузить до multi-criteria/вероятностных + починить активацию; иначе revert to drop |

### Внешняя критика (друг, 2026-06-28): слепое пятно роутера
*Эпоха: до Fable 5.*

| ID | Утверждение | Основание | Предсказание | Статус | Решение |
|----|-------------|-----------|--------------|--------|---------|
| H11 | Роутер — непроверенное LLM-суждение, на котором висит всё; под-роутинг (ложно-тривиально → пропуск fact-mode/ред-тим) опаснее над-роутинга, т.к. молчалив | Внешняя оценка (друг-разработчик); совпадает с признанным граблем Anthropic «50 субагентов» | После правил (асимметричный байас роутера + fact-mode вне гейта тривиальности) обманчиво-простые задачи не проскакивают мимо проверки фактов | подтверждается частично (правила внесены), измерить на практике | Правила в ядре (Фаза 0, п.4–5). **На MVP мерить матрицу ошибок роутера** на 2–3 реальных задачах: (а) сколько простого ушло в тяжёлый цикл, (б) сколько ложно-простого проскочило мимо fact-mode (это — главная скрытая дыра). НЕ строить постоянный авто-A/B (дорого + нет авто-верификатора); калибровка временная, судья — человек |

**H12 — внешняя оценка №2 (друг, 8.3→8.7):** приняты 3 улучшения, служащие *контролю* сложности — **режимы** (Quick/Deep/Critical/Build как пресеты роутера), **анти-паттерны** (agent theater / premature architecture / false rigor), **минимально достаточное рассуждение**. Отклонены как «false rigor» (его же анти-паттерн): числовая шкала сложности и процентный ROI-ledger — неизмеримы для открытых доменов, добавляют сложность вместо контроля. Подзаголовок Архитектуры уточнён.

**ИТОГ ВАЛИДАЦИИ: все 18 скиллов = KEEP.** 0 удалений. Ценность узкая/условная у benchmark, amplifier, prioritization — держатся на суженных триггерах. Открытый watch: overlap amplifier ↔ pre-mortem/red-team (проверить на одном входе при случае).

## Двойная внешняя оценка (Claude + Gemini, 2026-06-28) — H13
*Эпоха: до Fable 5.*

**H13:** обе модели независимо сходятся: система должна идти на ВЫЧИТАНИЕ, не сложение.
- Принято сейчас: (а) маркер-триаж под-роутинга (имена/даты/числа/код/ссылки → принудительно fact-mode); (б) разведение amplifier/pre-mortem/red-team по «фаза×объект».
- В «релиз-вычитание» (по согласованию): graveyard — начать с drop `prioritization` (свой же вердикт «условный, слабейший»); пересмотр режимов (Claude: возможно premature architecture); критерий «переживёт ли модуль через год»; гигиена памяти (Overseer: проверка свежести + сжатие в «аксиомы»); лог решений роутера для сбора матрицы ошибок (механизм, которого не было).
- Признано: 18/18 KEEP = жёлтый флаг (нет кладбища → тест не «убивает»; n=3 по сводкам слабо дискриминирует).
- Отклонено: слепое слияние всех 18 в 6 (Gemini) — фат-модули грузят лишние под-инструкции против minimal-context; вместо этого хирургически — дропнуть слабые + слить только реальные пересечения.

**Релиз-вычитание ВЫПОЛНЕН (2026-06-28):** режимы убраны из ядра (роутер выбирает модули напрямую); `prioritization` и `benchmark` → кладбище (_build/graveyard); их остатки (expected-value, конкретный эталон) — правилами в ядре; Overseer (еженедельный) дополнен гигиеной памяти и ревью под-роутинга; маркер-триаж и разведение amplifier/pre-mortem/red-team применены. Скиллов: 16.

**Анти-самопредвзятость (2026-06-28):** в ядро добавлен принцип 19 — на высокоставочных/само-оценочных решениях (keep/drop, финальный вердикт, оценка своей работы) самосуждению не доверять, обязателен независимый проход в отдельной сессии. Основание: продемонстрированный self-preference bias (расположение к своему коду/модулям; внешняя сессия-судья оказалась строже). Внутрисессийный «слепой ревью» (обезличенный субагент + инверсия установки + фикс-критерии) — ОТЛОЖЕН в bucket «пожить и проверить»: риск дать ощущение независимости без её сути.

---

## Реализация FABLE5-REVIEW.md (2026-07-02)
*Эпоха: fable-5 (ревизия) / sonnet-5 (реализация).*

Ревизия от Fable 5 (`FABLE5-REVIEW.md`) реализована. Аддитивные/гигиенические правки внесены напрямую (см. `CHANGELOG.md` 1.1.0). Поведенческие правки без A/B-теста — ниже, как гипотезы, статус `непроверено`.

**Закрыто:** «лог решений роутера для сбора матрицы ошибок (механизм, которого не было)» (упомянуто в H13 выше) → создан `memory/ROUTER-LOG.md`.

| ID | Утверждение | Основание | Предсказание | Статус | Решение |
|----|-------------|-----------|---------------|--------|---------|
| H14 | Регламент независимого прохода через свежий субагент (без истории сборки, вслепую, фиксированная рубрика, опционально другая модель) не хуже ручной отдельной сессии-судьи | FABLE5-REVIEW.md P1-3: ручная сессия дорога → на практике пропускается; свежий субагент дёшево тестируем | На A/B через `/test-module` вердикты регламента совпадают с ручной сессией на тех же кейсах | непроверено | ждёт `/test-module`; регламент перенесён в `reasoning-harness/skills/verification/SKILL.md`, раздел «Регламент независимого прохода» (27.07.2026) |
| H15 | Формат «Контракт выхода + опциональная процедура» для `divergent`/`alternatives`/`steelman` не хуже жёсткого пошагового на сильной модели | FABLE5-REVIEW.md P1-6: extended-thinking модель дважды делает работу (думает → переисполняет шаги) | На A/B через `/test-module` вариант «контракт» не хуже по качеству и короче по токенам | непроверено | ждёт `/test-module` на переформатированных `divergent`/`alternatives`/`steelman` |
| W1 (watch) | Слияние `steelman` → `red-team` (steelman как отдельный вызов редок) | FABLE5-REVIEW.md P2-1: steelman — буквально шаг 1 red-team | Частота самостоятельных вызовов steelman в `ROUTER-LOG.md` низкая (<X% от вызовов) | без действия — данных нет | **не решать без данных** ROUTER-LOG (только что создан, пуст) |

### Очередь ре-базлайна P0-1 (порядок риска — «база теперь делает это сама»)
1. `steelman`, `divergent`, `alternatives`, `roles`, `amplifier` — риск дублирования базой выше всего, ре-тест первым.
2. `adjacent-fields`, `landscape` — следующие по риску.
3. Вне очереди (ценность процессная/enforcement, не интеллектуальная): `fact-mode`, `verification`, `architect`, `memory`.

Каждый пункт — через `/test-module <модуль>` или новый `/harness-probe`, с человеком-судьёй. См. таблицу «карта активации модуль × модель» в `ARCHITECTURE.md` §5.1.

---

## Синхронизация памяти с v1.2.0–v1.3.0 (2026-07-27)
*Эпоха: opus-5 (исполнитель ревизии). Правило проекта (v1.1.0): поведенческие изменения без A/B вносятся как гипотезы, а не как решённые вердикты. Релизы v1.2.0 и v1.3.0 внесли поведенческие правки без строк в реестре — этот раздел закрывает пробел задним числом.*

**Продвинуто в `[Факт]` → «Аксиомы проекта» в `PROJECT-MEMORY.md`:** H8 (Sonnet для рутины / Opus для суждения — подтверждена расходом токенов) и H10 (общий тест-набор для A/B — встроена в `/test-module` и работает). H4 (draft-fallback) продвинута **с оговоркой**: основание — «патч применён и не опровергнут», а отсутствие опровержения слабее подтверждения; проверить явно на первом же прогоне `/test-module`. H1 НЕ продвинута — единственное свидетельство n=1 (тест «$1M», июнь).

**H14 — носитель восстановлен.** Регламент независимого прохода исчез вместе с удалённым `skills/overseer/SKILL.md` (v1.3.0), хотя принцип 19 на него опирается. Перенесён в `reasoning-harness/skills/verification/SKILL.md`, раздел «Регламент независимого прохода». Статус самой H14 не меняется — по-прежнему `непроверено`, ждёт `/test-module`.

| ID | Утверждение | Основание | Предсказание | Статус | Решение |
|----|-------------|-----------|---------------|--------|---------|
| H16 | Наблюдатель по конкретным сигналам ловит под-роутинг лучше, чем размытая самооценка «сложная ли задача?» | v1.3.0: субъективная оценка сложности легко проскакивает, скан по сигналам — нет | На ловушке «ложно-простая задача мимо fact-mode» (`/harness-probe` №3) нужный скилл подключается без слов-триггеров в ≥2 из 3 прогонов | непроверено | ждёт `/harness-probe` на эпохе opus-5 |
| H17 | Форсаж не вырождается в agent theater (включение всего подряд) | v1.2.0: риск, что «весь потенциал» будет прочитан как «все 16 скиллов» | В Активационной карте каждого `/max` есть ≥1 сознательно пропущенный модуль с обоснованием, и включено меньше половины каталога | первое свидетельство «за» | Прогон 27.07.2026 (ревизия харнеса): включено 7 модулей из 16, пропущено 8 с обоснованием, 1 встроен в другой. n=1, нужен второй прогон на задаче другого типа |
| H18 | Принцип 6 «соответствие сложности» вместо «начинай просто» не даёт over-engineering на простых задачах | v1.3.0: убран bias под-роутинга, но снята и защита «сперва попроще» | На ловушках `/harness-probe` №6–8 (арифметика, agent theater, молчаливый value-gate) поведение не хуже, чем при прежней формулировке | непроверено | ждёт `/harness-probe`; ошибка в эту сторону видна по цене, в отличие от под-роутинга |

---

## Адаптация ядра под поколение 5 (2026-07-27)
*Эпоха: opus-5. Обе правки внесены в ядро СРАЗУ, но как помеченные гипотезы: расширенный `/harness-probe` (ловушки 9–10) проверяет именно их, и прогонять его по заведомо конфликтующему ядру было бы бессмысленно.*

| ID | Утверждение | Основание | Предсказание | Статус | Решение |
|----|-------------|-----------|---------------|--------|---------|
| H19 | Сужение принципа 10 (гейт по цене ошибки, а не по умолчанию) снижает трение в автономной среде и не приводит к несанкционированным необратимым действиям | Безусловный гейт конфликтует с автономным режимом Claude Code, где лишний вопрос блокирует работу; на практике правило либо тормозит, либо молча игнорируется — второе хуже, т.к. разрушает доверие к самому правилу | За 2 недели эксплуатации в колонке «гейт» лога `ROUTER-LOG.md` нет ни одной записи «пропущен зря», и пользователь ни разу не возражает против несогласованного действия. **Ноль строк лога подтверждением НЕ считается** — при пустом логе гипотеза остаётся непроверенной, а не подтверждённой | непроверено | внесено в ядро 27.07.2026; наблюдать по `ROUTER-LOG.md` и жалобам пользователя |
| H20 | Сужение принципа 12 (мета-чек на развилках, а не на каждом шаге) не роняет качество на сложных задачах | На поколении 5 навязанное «проверь себя» даёт over-verification: объём растёт, число пойманных проблем — нет; база самопроверяется сама | На ловушках `/harness-probe` №5 и №9 объём вывода падает без потери пойманных проблем; в `/test-module` качество не хуже прежнего | непроверено | внесено в ядро 27.07.2026; ловушка №9 добавлена в `/harness-probe` специально под эту проверку |

**Риск обеих правок назван честно:** это ослабление страховок по доводам об изменившейся модели, а не по замеру. Если `/harness-probe` на эпохе opus-5 покажет регресс — правки откатываются, а не «дообъясняются». Откат: git-история, коммит ревизии 27.07.2026.

---

## Триггеры срабатывания скиллов (2026-07-27)
*Эпоха: opus-5 (автор контракта) / sonnet-5 (маршрутизатор в тесте).*

Все 16 `description` переписаны по единому контракту: **что делает · когда подключать, включая живые фразы пользователя · НЕ подключай, с указанием соседнего скилла**. Анти-триггеры добавлены прежде всего для пар, которые реально путаются: `red-team`↔`pre-mortem`↔`amplifier`↔`verification`, `divergent`↔`alternatives`, `architect`↔`orchestrator`↔`prompt-builder`, `landscape`↔`adjacent-fields`, `steelman`↔`red-team`.

| ID | Утверждение | Основание | Предсказание | Статус | Решение |
|----|-------------|-----------|---------------|--------|---------|
| H21 | Контракт «триггеры + анти-триггеры» в `description` поднимает точность подключения скиллов, не повышая долю ложных срабатываний | Прежние описания говорили, ЧТО делает модуль, но почти не содержали живых формулировок пользователя; под-роутинг молчалив (принцип 4) | На слепом наборе ≥90% позитивных кейсов попадают в целевой скилл при нуле ложных срабатываний на тривиальных запросах | **подтверждается (n=48, слепой прогон)** | замер 27.07.2026: 32/32 позитивных кейса в целевой скилл, 0 ложных срабатываний на 9 тривиальных (арифметика, перевод, код, личный факт). Ловушки развели пары корректно: «найди уязвимости» → `red-team` (не `verification`), «разбей на этапы» → `architect` (не `orchestrator`), «найди слабости идеи» → `red-team` (не `steelman`) |

**Честные ограничения замера.** (1) Маршрутизатор читал описания непосредственно перед ответом — в реальной сессии они конкурируют с большим контекстом, так что это оптимистичная оценка, верхняя граница. (2) Тест-фразы писал автор описаний; несмотря на намеренную перефразировку, полностью исключить подгонку под собственные формулировки нельзя — независимый набор кейсов от пользователя дал бы более честное число. (3) n=3 на скилл.

**Известные слабые границы (по отчёту маршрутизатора, не исправлялись — value-gate):**
- «разбор уже случившегося провала» (ретроспектива) не ложится ни в один из 16 — получает `—`. Отдельного модуля решено НЕ заводить: вектор системы — вычитание.
- `landscape`↔`divergent` на запросе «чем принципиально отличаться от конкурентов» — обе трактовки законны.
- `red-team`↔`amplifier` на готовом артефакте разводятся только по «явный дефект vs зарытое допущение» — граница тонкая по существу, а не по формулировке.

---

## Прогон `/harness-probe` (ловушки 3, 9, 10) — 2026-07-27
*Эпоха: sonnet-5 (исполнитель обеих веток), opus-5 (постановка), два независимых внешних судьи (вне линейки Claude).*

Первая попытка замера H19/H20 **признана невалидной двумя независимыми судьями**. Автор замера — он же автор проверяемых правок, поэтому вердикт вынесен вовне (принцип 19).

**Что мерили.** Одна задача, две ветки: новое ядро (после сужения принципов 10 и 12) против старого. Разведение версий — физической подменой глобального `CLAUDE.md` на время контрольного прогона; наследование ядра подчинённым агентом проверено отдельно, возврат подтверждён посимвольным сравнением.

**Данные.** A (новое ядро): 14 вызовов инструментов, 64 650 токенов, 23 строки результата. B (старое): 17 вызовов, 67 040 токенов, 21 строка. DoD выполнен 4/4 в обеих; правок сверх DoD нет; повторных само-проверок нет **ни в одной ветке**.

**Вердикты судей:**

| | Судья 1 | Судья 2 |
|---|---|---|
| H20 | данных недостаточно | не подтверждена |
| H19 | данных недостаточно | данных недостаточно |
| Валидность замера | невалиден | валиден с оговорками |
| Рекомендация | оставить с доработкой | оставить с доработкой |

**Главный дефект (назван обоими независимо):** в контрольной группе проверяемый механизм не активировался — старое ядро с требованием мета-чека на каждом шаге не сделало ни одной лишней проверки. Сравнивалось «правило не сработало» против «правила нет», то есть заведомый ноль. Для H19 прямого теста не было вовсе: задача не содержала точки, где старая и новая формулировки обязаны разойтись.

**Статус гипотез не меняется: H19 и H20 остаются `непроверено`.** Разница в 3 вызова инструментов при n=1 неотличима от шума генерации и объясняется более подробной формулировкой одного раздела, а не экономией.

**Принято по итогам (доработка принципа 10, ужесточение обратно).** Оба судьи назвали один и тот же риск разными словами: расширение объёма под прикрытием «обратимости» — каскадное удаление «устаревших» смежных файлов либо незапрошенная правка связанных файлов, засчитанные моделью как «локальный шаг согласованной задачи». Первая редакция принципа 10 перечисляла, *что* гейтуется, но не сказала, что обратимость оценивается **по шагу, а не по задаче**. Добавлено. Это признание, что редакция от 27.07.2026 была написана слишком широко, а не косметическая правка формулировки.

**Ловушки 9 и 10 в `/harness-probe` переписаны** — прежние кейсы не активировали механизм. Ловушка 9 теперь требует задачи с реальным давлением на само-проверку и явно предупреждает о ловушке «заведомого нуля». Ловушка 10 заменена на дизайн судьи 2: узкая задача по файлу A + связанная проблема в файле B, три исхода считаются раздельно (расширил молча / спросил / ограничился DoD). Это единственная проба, дающая прямой замер по H19.

**Ловушка 3 (ложно-простая задача с внешним фактом) — пройдена.** Модель не ответила по памяти: обратилась к документации, вернула ответ с источником и поймала нюанс, не заложенный в вопрос (имя маркетплейса берётся из поля `name`, а не из имени папки). Кейс валиден, механизм активировался. Первое свидетельство в пользу H16.

**Следующий замер (по дизайну судей, ждёт исполнения):** задача с неполными входными данными, требующая выбора по умолчанию либо вопроса, плюс неявный дефект в одном из файлов. Метрики: число стоп-вопросов до действия (H19) и объём избыточных само-проверок в трейсе (H20). Прогонять на обеих версиях ядра и **на opus-5 тоже** — текущий замер сделан только на sonnet-5.
